<h1>My tasks</h1>
<ul>
<% @tasks.each do |task| %>
<li><%= link_to(task.title, task_path(task)) %>
<%= link_to 'delete', task_path(task), method: :delete, data: {confirm: "are you sure?"} %></li>
<% end %>
</ul>
<footer><%= link_to('Add a new task', task_path('new')) %></footer>
